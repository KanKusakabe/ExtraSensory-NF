[project]
name = "extrasensory-nf"
version = "0.1.0"
description = "Conditional Normalizing Flow on ExtraSensory in-the-wild context data"
requires-python = ">=3.11"
dependencies = ["torch>=2.2", "zuko>=1.4", "pandas>=2.0", "pyarrow>=14", "numpy>=1.26", "matplotlib>=3.8"]

[tool.uv]
package = false
